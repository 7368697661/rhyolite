/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Removes the small bottom-left Next.js dev indicator ("n").
  devIndicators: false,
};

export default nextConfig;

