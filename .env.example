GEMINI_API_KEY="your_gemini_api_key"

# Optional: absolute path to the workspace directory where project folders are stored.
# Defaults to <repo>/.workspace
# WORKSPACE_DIR="/path/to/rhyolite-projects"

